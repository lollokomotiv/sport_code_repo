export default function ComingSoon({ title }: { title: string }) {
  return (
    <div>
      <h1 className="mb-2 text-xl font-semibold">{title}</h1>
      <p className="text-neutral-500">Questa sezione arriverà nelle prossime fasi del frontend.</p>
    </div>
  )
}
